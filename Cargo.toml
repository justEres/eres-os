[package]
name = "eres-os"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[features]
default = []
qemu-test = []

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
